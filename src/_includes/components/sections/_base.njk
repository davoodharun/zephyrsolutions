{# Base section component template - used as reference for section structure #}
{# Each section component should follow this pattern: #}
{# - Accept config parameter with section-specific fields #}
{# - Use semantic HTML elements #}
{# - Include proper accessibility attributes #}
{# - Use CSS classes from design system #}

<section class="section section--{{ type }}">
  {# Section content goes here #}
</section>
